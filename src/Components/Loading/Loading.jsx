import React from 'react';

const Loading = () => {
  return (
    <section className="container mx-auto flex justify-center mt-50">
      <span className="loading loading-spinner text-primary w-10"></span>
    </section>
  );
};

export default Loading;
